class Round {

    static float counter = 0;

    static float playRound(Shoe shoe, Rules rules) {

        counter++;
        
        return counter;

        // Hand dealer = new Hand();
        // Hands player = new Hands();
        // int numOfSplits = 0;

        // deal(shoe, dealer, player);

        // while (player.hasActionableHands) {


        // }

        // ArrayList<Hand> playersBets = new ArrayList<Bet>();
        // player.addBet(new Hand(5));
        // dealCards();
        // while (playersBets.hasActionableBets()) {
        //     Action action = getAction(bet);
        //     if (action == Action.SPLIT) {
        //         // do the thing
        //     }
        // }
    }

}
